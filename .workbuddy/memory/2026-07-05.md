## 2026-07-05

### FlowForge 多语言方案设计
- 完成 `deliverables/flowforge-i18n-design.md` 技术方案文档
- 关键决策：业务节点不翻译（团队内单一业务语言），仅翻译系统 UI 壳层
- 技术选型：自建轻量 i18n（零依赖），useSyncExternalStore + TS 翻译字典，不用 i18next
- 翻译范围：6 个组件 ~50 条文本（Toolbar/NodeLibrary/PropertyPanel/ParametersPanel/StatusBar/canvas 右键菜单）
- 语言切换入口：Toolbar 右侧下拉选择器
- 改动量：纯增量 ~220 行，不改现有架构

### FlowForge 多语言功能实现
- 创建 i18n 核心模块：`src/i18n/` (types.ts + zh-CN.ts + en-US.ts + index.ts)
- 改造 9 个组件文件：Toolbar / NodeLibrary / PropertyPanel / ParametersPanel / StatusBar / FlowCanvas / FlowNodeComponent / LandingScreen / TypePickerModal
- Toolbar 新增语言切换 `<select>`，在当前语言下显示选项名（中文→"中文"/"English"，English→"简体中文"/"English"）
- 修正 `as const` 导致英文字典值类型冲突，改为普通 object（key 类型仍然正确推导）
- tsc 零错误，vite build 通过 (176 modules)

### FlowForge 流类型系统实现
- 新增 FlowType 概念（剧情流/任务流/动作流），作为节点过滤的顶层抽象
- 首场景重构：LandingScreen（新建选择类型 + 最近文件）→ Editor 的双屏架构
- 新建 `store/appStore.ts`：screen 路由 + recentFiles（localStorage 持久）
- 新建 `data/flowTypes.ts`：内置 3 种流类型
- 新建 `screens/LandingScreen.tsx` + `components/modals/TypePickerModal.tsx`
- 修改 `types/index.ts`：FlowType 接口、NodeDefinition.flowTypes、FlowMeta.flow_type
- 修改 `data/builtinNodes.ts`：所有节点分配 flowTypes
- 修改 `store/editorStore.ts`：flowType 状态、setFlowType（过滤+重建默认节点）
- 修改 `components/toolbar/Toolbar.tsx`：去掉 Open/Sample，新增 Home 按钮 + 流类型标签
- 修改 `App.tsx`：基于 appStore.screen 路由 LandingScreen ↔ AppShell
- 修改 `api/fileApi.ts`：新增 loadFile(path) 直接读取（用于最近文件回开）
- 修改 `components/layout/AppShell.tsx`：序列化加 flow_type，保存时写 recentFiles
- TypeScript 编译通过，零错误
- 新增 FlowType 概念（剧情流/任务流/动作流），作为节点过滤的顶层抽象
- 首场景重构：LandingScreen（新建选择类型 + 最近文件）→ Editor 的双屏架构
- 新建 `store/appStore.ts`：screen 路由 + recentFiles（localStorage 持久）
- 新建 `data/flowTypes.ts`：内置 3 种流类型
- 新建 `screens/LandingScreen.tsx` + `components/modals/TypePickerModal.tsx`
- 修改 `types/index.ts`：FlowType 接口、NodeDefinition.flowTypes、FlowMeta.flow_type
- 修改 `data/builtinNodes.ts`：所有节点分配 flowTypes
- 修改 `store/editorStore.ts`：flowType 状态、setFlowType（过滤+重建默认节点）
- 修改 `components/toolbar/Toolbar.tsx`：去掉 Open/Sample，新增 Home 按钮 + 流类型标签
- 修改 `App.tsx`：基于 appStore.screen 路由 LandingScreen ↔ AppShell
- 修改 `api/fileApi.ts`：新增 loadFile(path) 直接读取（用于最近文件回开）
- 修改 `components/layout/AppShell.tsx`：序列化加 flow_type，保存时写 recentFiles
- TypeScript 编译通过，零错误
