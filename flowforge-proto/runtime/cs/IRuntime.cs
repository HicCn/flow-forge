namespace FlowForge.Runtime;

/// <summary>
/// Minimal runtime context passed to node lifecycle methods.
/// Extend this interface per engine (Godot, Unity, etc.).
/// </summary>
public interface IRuntime
{
}
